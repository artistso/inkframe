package com.inkframe.feature.timeline

/**
 * Placeholder for the timeline feature module.
 *
 * The timeline UI currently lives inline in `:feature-canvas` (`StudioScreen`'s
 * `TimelineBar`). This module is reserved for extracting a richer, standalone timeline:
 * exposure/hold editing, cel copy/paste & drag-move, multi-frame onion-skin controls,
 * and FPS scrubbing. See docs/ARCHITECTURE.md (roadmap §6.6).
 */
internal object TimelineModule
